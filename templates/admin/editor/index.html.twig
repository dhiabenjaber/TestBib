{% extends "admin/base.html.twig" %}
{% block body %}
    <div class="d-flex justify-content-between w-100" id="navbarSupportedContent">
        <div class="d-flex">
            <h3>editors</h3>
        </div>
        <button class="btn btn-sm btn-secondary" type="button" onclick="location.href='{{ path("admin_editors_new") }}';">Add <span class="fas fa-plus sm-2"></span></button>
    </div>
    <br/>
    <br/>
    <div id="example_wrapper" class="dataTables_wrapper dt-bootstrap5">
        <table id="example" class="table table-striped" style="width:100%">
            <thead>
            <tr>
                <th>Name</th>
                <th>Country</th>
                <th>Address</th>
                <th>Telephone</th>
                <th class="text-center">Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for editor in editors %}
                <tr>
                    <td>{{ editor.name }}</td>
                    <th>{{ editor.country }}</th>
                    <th>{{ editor.address }}</th>
                    <th>{{ editor.telephone }}</th>
                    <td class="text-center"><div class="btn-group">
                            <button type="button" class="btn btn-sm dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                    <span class="icon icon-sm">
                                                        <span class="fas fa-ellipsis-h icon-dark"></span>
                                                    </span>
                                <span class="sr-only">Toggle Dropdown</span>
                            </button>

                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item text-black btn-labeled" href="{{ path("admin_editors_show",{"id":editor.id}) }}"><span class="btn-label"><i class="fa fa-eye"></i></span> Show</a></li>
                                <li><a class="dropdown-item text-black" href="{{ path("admin_editors_edit",{"id":editor.id}) }}"><span class="btn-label"><i class="fa fa-edit"></i></span> Edit</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item text-danger" href="#" onclick="dialogue({{ loop.index-1 }},'{{path('admin_editors_delete',{"id":editor.id})}}')"><span class="btn-label"><i class="fa fa-trash"></i></span> Delete</a></li>
                            </ul>
                        </div>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
            <tfoot>
            <tr>
                <th>Name</th>
                <th>Country</th>
                <th>Address</th>
                <th>Telephone</th>
                <th class="text-center">Actions</th>
            </tr>
            </tfoot>
        </table>
    </div>
{% endblock %}